배열 -> data type -> int, float, double  => 기본 데이터 타입
 ㄴ파생 데이터형 - 배열     )  배열과 포인터는 밀접한 관계가 있음. 또한 밑에 조건이 중요시 됨
	          ㄴ포인터 )  참조타입(java)

정의 -> ⭐변수들의 집합⭐ -> 조건 -> 1. 메모리에 연속적으로 모여있어야 함
			       2. 같은 데이터 타입의 변수들
                                              ㄴ 위와 같은 것이 아니면 구조체라고 부름 -> struct  

사용: 선언 => 데이터 타입 _(한칸 띄우고) 배열명[크기]  => ex) doeble B[80]; - 해당 배열은 8byte 가 10개 즉, 80byte 
					 ㄴ변수가 몇개 모여있는지
					 ㄴ주의할 점은 0보다 큰 정수형 상수로 됨
인덱스
ㄴ배열의 각 방을 접근하는 번호
ㄴ0번부터 시작 - 크기는 -1방

ex) int arr[5]
	arr[3] = 10; - arr[]는 변수 취급으로 10을 대입하는 구조임
	원래는 안되는데 C언어는 받아줌 이는 곧 buffer overflow 이 됨

배열과 for문은 반드시 붙어다닌다. 예시로 만 개의 칸에 숫자를 다 집어넣으려면 인덱스를 만 번 해야 됨
또한 배열의 차수와  for의 중첩 차수는 일반적으로 같다.

